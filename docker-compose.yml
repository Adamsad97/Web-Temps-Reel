services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=development
      - PORT=4000
      - JWT_SECRET=avenir_bank_super_secret_jwt_2024
      - FRONTEND_URL=http://localhost:3000
      - VAPID_PUBLIC_KEY=BNsGLcL5HXHELz9S7eXti4Gi5kpFo7CX_LAjhE2nhQYRPLAn28wbqTZY_HKKflsXsDzK14mAw5fuOB57qX7wBM4
      - VAPID_PRIVATE_KEY=6oNVseHKsWM-YBVTZWHqbGmfbx1LkBDd7IAECAcIo60
      - VAPID_SUBJECT=mailto:admin@avenir.fr
    volumes:
      - ./backend/src:/app/src
    networks:
      - avenir-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:4000
      - NEXT_PUBLIC_WS_URL=http://localhost:4000
      - NEXT_PUBLIC_VAPID_PUBLIC_KEY=BNsGLcL5HXHELz9S7eXti4Gi5kpFo7CX_LAjhE2nhQYRPLAn28wbqTZY_HKKflsXsDzK14mAw5fuOB57qX7wBM4
      - INTERNAL_API_URL=http://backend:4000
    depends_on:
      - backend
    volumes:
      - ./frontend/src:/app/src
    networks:
      - avenir-network

networks:
  avenir-network:
    driver: bridge
